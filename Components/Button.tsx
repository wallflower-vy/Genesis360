import Link from 'next/link'
import React from 'react';
import { useRouter } from 'next/router';


interface Props {
   
   
  }
const Button = () => {
  return (
    <div>
     {/* <Link {...rest}>
      <a className={activeClass} onClick={() => router.push(rest.href)}>
        {text}
      </a>
    </Link> */}
    </div>
  )
}

export default Button
